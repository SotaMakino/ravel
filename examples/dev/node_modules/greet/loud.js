// A relative import inside a package: it resolves against this file's real
// URL, which only works because /@id/ redirects rather than proxying.
import { hello } from "./browser.js";
export const shout = (name) => hello(name).toUpperCase();
